# Save this as .streamlit/secrets.toml (exact path: a folder named
# ".streamlit" containing a file named "secrets.toml") next to app.py.
#
# If deploying on Streamlit Community Cloud: do NOT upload this file.
# Instead go to your app -> Settings -> Secrets, and paste the block below
# (without the surrounding comment lines) into that box.
#
# This is the SESSION POOLER string (port 5432) — used because Streamlit
# is a long-running process that keeps a connection open across reruns.

[connections.db]
url = "postgresql://postgres.actigmterpcovsdcypxa:%2F%3FwjkT5pS3K%40%2F%25Q@aws-0-eu-west-1.pooler.supabase.com:5432/postgres"

[
intasend
]
public_key = "ISPubKey_test_9c56838d-f19c-4dce-b691-e2da21e30f5e"
secret_key = "ISSecretKey_test_d6308abb-3ca0-4f23-88f7-b686ca5ee202"